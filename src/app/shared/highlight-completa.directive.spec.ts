import { HighlightCompletaDirective } from '../highlight-completa.directive';

describe('HighlightCompletaDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletaDirective();
    expect(directive).toBeTruthy();
  });
});
